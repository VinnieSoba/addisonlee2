import styled from "styled-components";
import ActionLink from "./ActionLink";

const FloatingActionLink = styled(ActionLink)`
  float: right;
  margin: 0;
  text-decoration: underline;
`;

export default FloatingActionLink;
